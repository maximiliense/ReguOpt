# Suppress the DEP0205 deprecation warning emitted by @tailwindcss/node@4.x on
# Node.js v23+. That package still uses module.register() internally; the
# warning is harmless and will disappear once Tailwind ships a fix upstream.
node-options=--disable-warning=DEP0205
